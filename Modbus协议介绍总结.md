# Modbus 协议总结

## 一、Modbus 是什么

**Modbus** 是**工业自动化领域应用最广泛的串行通信协议**，由 Modicon 公司（现施耐德旗下）于 **1979** 年开发，最初用于 PLC 通信，现已成为工业通讯领域的事实标准。

Modbus 是一种**主从问答式**通信协议，特点是简单、可靠、开放，几乎所有工业设备厂商都支持，广泛应用于各种工业自动化场景。

## 二、主要传输模式

Modbus 有两种主要传输方式：

### 1. Modbus RTU（Remote Terminal Unit）

- **物理层**：串口（RS-485 / RS-232 / RS-422）
- **传输介质**：双绞线
- **特点**：二进制编码，紧凑的数据格式，CRC 校验
- **应用场景**：现场仪表（电表、水表、流量计、热表、称重仪）、变频器、伺服、调节阀等工业设备通信

### 2. Modbus TCP

- **物理层**：以太网（RJ45）
- **传输层**：TCP/IP，默认端口 **502**
- **特点**：在 Modbus RTU 基础上增加 MBAP 报文头，运行于以太网上
- **应用场景**：以太网设备通信，PLC 之间通信，与 DCS/SCADA 系统通信

### RTU vs TCP 对比

| 对比项 | Modbus RTU | Modbus TCP |
|--------|------------|------------|
| 物理层 | 串口（RS-485） | 以太网 |
| 传输介质 | 双绞线 | 网线/光纤 |
| 校验方式 | CRC 校验 | TCP 校验 |
| 最大节点数 | 32 个（需中继） | 几乎无限制 |
| 传输速率 | 最高 1Mbps | 可达 1Gbps |
| 传输距离 | 最长 1200 米 | 很远（可跨网） |

## 三、数据模型

Modbus 定义了四种基本寄存器类型：

| 寄存器类型 | 读写性 | 数据类型 | 常用功能码 | PLC 地址范围 | 说明 |
|------------|--------|----------|------------|--------------|------|
| **线圈寄存器** | 读/写 | 位（bit） | 01H, 05H, 0FH | 00001-09999 | 开关量输出，可控制设备启停 |
| **离散输入寄存器** | 只读 | 位（bit） | 02H | 10001-19999 | 开关量输入，读取外部触点状态 |
| **保持寄存器** | 读/写 | 字（2字节） | 03H, 06H, 10H | 30001-39999 | 模拟量输出/存储参数，可写 |
| **输入寄存器** | 只读 | 字（2字节） | 04H | 40001-49999 | 模拟量输入，读取测量值 |

**说明**：
- 每个线圈/离散输入占 **1 bit**，一个字节可存储 8 个开关量
- 每个保持/输入寄存器占 **2 字节（16 位）**
- 多字节数据需要多个寄存器拼接

## 四、常用功能码

| 功能码 | 名称 | 操作 | 作用 |
|--------|------|------|------|
| **01H** | 读线圈寄存器 | 读 | 读取多个线圈的 ON/OFF 状态 |
| **02H** | 读离散输入寄存器 | 读 | 读取多个离散输入的状态 |
| **03H** | 读保持寄存器 | 读 | 读取多个保持寄存器的值 |
| **04H** | 读输入寄存器 | 读 | 读取多个输入寄存器的值 |
| **05H** | 写单个线圈寄存器 | 写 | 设置单个线圈的状态 |
| **06H** | 写单个保持寄存器 | 写 | 写入单个保持寄存器 |
| **0FH** | 写多个线圈寄存器 | 写 | 设置多个线圈的状态 |
| **10H** | 写多个保持寄存器 | 写 | 写入多个保持寄存器 |

## 五、报文格式

### Modbus RTU 报文格式

```
+----------+----------+----------------+----------+----------+
| 从站地址 | 功能码  |    数据域      |   CRC高位 |  CRC低位 |
|  1字节   |  1字节   |    N字节       |   1字节  |   1字节  |
+----------+----------+----------------+----------+----------+
```

**举例 - 读保持寄存器（功能码 03H）：**

发送：
```
从站地址(01) + 功能码(03) + 起始地址(00 00) + 数量(00 02) + CRC(xx xx)
```

响应：
```
从站地址(01) + 功能码(03) + 字节数(04) + 数据(N字节) + CRC(xx xx)
```

### Modbus TCP 报文格式

Modbus TCP 由 **MBAP 头 + PDU** 组成，取消了 CRC 校验（由 TCP 层负责）：

```
+------------------+----------------+------------+--------------+----------+
| 事务处理标识(2B) | 协议标识(2B)  | 长度(2B)  | 单元标识符(1B) | PDU  |
+------------------+----------------+------------+--------------+----------+
```

字段说明：
- **事务处理标识**：报文序列号，每次递增，用于匹配请求响应
- **协议标识**：固定为 `00 00` 表示 Modbus TCP
- **长度**：后续数据长度（字节数）
- **单元标识符**：等价于 RTU 中的从站地址
- **PDU**：功能码 + 数据，与 RTU 格式类似

## 六、通信模型

- **主站（Master）**：发起请求，主动查询数据
- **从站（Slave）**：被动响应，回复请求的数据
- 支持一主多从，同一总线上多个从站设备通过地址区分

## 七、解决了什么问题

1. **统一标准**：不同厂商设备遵循相同协议，可互相通信
2. **简单易用**：协议精简，容易实现，资源占用小
3. **成熟稳定**：经过几十年工业现场验证，可靠性高
4. **广泛支持**：几乎所有工业设备都原生支持，开发成本低

## 八、应用场景

- **工业自动化**：PLC 与传感器/执行器通信
- **能源计量**：电表、水表、燃气表数据采集
- **楼宇自动化**：HVAC、照明、消防系统监控
- **物联网**：低速率传感数据采集
- **电力监控**：变电站内智能设备数据采集

## 九、GitHub 上相关开源项目

| 项目 | 语言 | Stars | 说明 |
|------|------|-------|------|
| [stephane/libmodbus](https://github.com/stephane/libmodbus) | C | 4101 | 支持 Linux/Mac/Windows 的 C 语言库，支持 RTU/TCP |
| [pymodbus-dev/pymodbus](https://github.com/pymodbus-dev/pymodbus) | Python | 2671 | 完整的 Python 实现 |
| [goburrow/modbus](https://github.com/goburrow/modbus) | Go | 1027 | Go 语言实现，容错性好 |
| [digitalpetri/modbus](https://github.com/digitalpetri/modbus) | Java | - | Java 版本 Modbus TCP/RTU 栈 |

## 十、总结

Modbus 是**工业领域最成功、应用最广泛的通信协议之一**，它：

- ✅ **简单**：协议精简，容易理解和实现
- ✅ **开放**：无版权问题，免费使用
- ✅ **可靠**：数十年工业验证，稳定性好
- ✅ **普及**：几乎所有工业设备都支持
- ✅ **灵活**：支持串口（RTU）和以太网（TCP）两种传输方式

Modbus 至今仍是工业数据采集和设备互联的首选协议之一。
